import 'package:flutter/material.dart';

const iconSize = 30.0;
const drawerMenuIconSize = 40.0;
const drawerMenuIconColor = Colors.white;

const filterIcon = Icon(Icons.filter_list_outlined, size: iconSize);
const playIcon = Icon(Icons.play_arrow, size: iconSize);
const menuIcon = Icon(Icons.menu, size: iconSize);
const languageIcon = Icon(
  Icons.android_rounded,
  size: drawerMenuIconSize,
  color: drawerMenuIconColor,
);
const statistics = Icon(
  Icons.stacked_line_chart_rounded,
  size: drawerMenuIconSize,
  color: drawerMenuIconColor,
);
